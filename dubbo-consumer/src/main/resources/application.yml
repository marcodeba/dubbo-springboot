spring:
  application:
    name: dubbo-consumer
server:
  port: 8080
dubbo:
  application:
    name: dubbo-consumer
  protocol:
    name: dubbo
    port: 20880
  registry:
    address: zookeeper://localhost:2181
  consumer:
    timeout: 5000
